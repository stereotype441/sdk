List<int> findPrimes(int last) {
  for (int x = 2; x < last; x++) {
    bool isPrime = true; // KEEP?
    if (p * p > x) { // KEEP?
      break; // KEEP?
    } // KEEP?
    isPrime = false; // KEEP?
  }
}
